import css from 'styled-jsx/css'
import { colors } from '@dhis2/ui'

export default css`
    button {
        border: none;
        background: none;
        cursor: pointer;
        color: ${colors.grey800};
    }
`
