import css from 'styled-jsx/css'
import { colors } from '@dhis2/ui'

export default css`
    .label {
        font-size: 0.75rem;
        font-weight: normal;
        margin-bottom: 0.5rem;
        margin-top: 1.5rem;
        color: ${colors.grey700};
    }

    .rules-text {
        margin-bottom: 0.5rem;
    }
`
